{
  "config": {
    "step": {
      "user": {
        "title": "Add app to track",
        "description": "Track the version of an app from the Apple App Store or Google Play.",
        "data": {
          "platform": "Store",
          "app_id": "App identifier",
          "country": "Country code",
          "scan_interval": "Update interval (minutes)"
        },
        "data_description": {
          "app_id": "App Store: numeric ID (e.g. 544007664) or bundle ID (e.g. com.google.ios.youtube). Google Play: package name (e.g. com.google.android.youtube).",
          "country": "Two-letter ISO country code (e.g. us, cz, gb)."
        }
      }
    },
    "abort": {
      "already_configured": "This app is already being tracked for the selected country."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Update settings",
        "data": {
          "country": "Country code",
          "scan_interval": "Update interval (minutes)"
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "version": {
        "name": "Version"
      },
      "last_refresh": {
        "name": "Last refresh"
      }
    },
    "button": {
      "refresh": {
        "name": "Refresh now"
      }
    }
  },
  "issues": {
    "fetch_failed": {
      "title": "Couldn't fetch {app_id} from {platform}",
      "description": "Home Assistant couldn't fetch metadata for **{app_id}** in **{country}** from {platform}. The integration will keep retrying. Common causes:\n\n- The app isn't published in this country\n- The identifier (numeric ID, bundle ID, or package name) is wrong\n- {platform} changed its response format and the parser needs updating\n\nLast error: `{error}`"
    }
  }
}
